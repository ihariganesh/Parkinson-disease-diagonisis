# FastAPI Backend Dependencies - Python 3.13 Compatible
# Core dependencies for the Parkinson's Detection App Backend

# ===== WEB FRAMEWORK =====
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# ===== AUTHENTICATION & SECURITY =====
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# ===== DATABASE =====
sqlalchemy>=2.0.35  # Updated for Python 3.13 compatibility
pydantic-settings    # Required for config management

# ===== DATA VALIDATION =====
email-validator      # Required for pydantic email validation

# ===== HTTP CLIENT & FILE HANDLING =====
httpx==0.25.2
requests==2.31.0
aiofiles==23.2.1

# ===== DATA PROCESSING (Python 3.13 compatible) =====
numpy                # Latest compatible version
pillow               # Image processing
scipy                # Scientific computing

# ===== NOTES =====
# - pandas 2.1.3 not compatible with Python 3.13 (compilation errors)
# - PostgreSQL drivers (psycopg2-binary) available but not needed for SQLite
# - ML libraries (tensorflow, torch, etc.) may have compatibility issues with Python 3.13
# - For production, consider using Python 3.11 or 3.12 for better ML library support